# Winnow data scraper — data-only (no scan engine). Pandas ingest + R2 sync.
pandas>=2.0
pyarrow>=15.0
requests>=2.31   # NSE bhavcopy fetch
yfinance>=0.2.40 # US bars + corporate-action (split/bonus) confirmation
boto3>=1.34      # R2 (S3-compatible) store sync
